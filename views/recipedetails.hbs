<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{recipe.recipiname}}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>

    <div class="container mt-4">
        <h2 class="text-center">{{recipe.recipiname}}</h2>
        <div class="card mx-auto" style="width: 25rem;">
            <img src="/uploads/{{recipe.imgname}}" class="card-img-top" alt="{{recipe.recipiname}}">
            <div class="card-body text-center">
                <h4 class="card-title">{{recipe.recipiname}}</h4>
                <p class="card-text">Price: ${{recipe.price}}</p>
                <form action="/order" method="POST">
                    <input type="hidden" name="food" value="{{recipe.recipiname}}">
                    <input type="hidden" name="price" value="{{recipe.price}}">
                    <input type="hidden" name="img_name" value="{{recipe.imgname}}">
                    <button type="submit" class="btn btn-success">Order</button>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
